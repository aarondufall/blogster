<% @posts.each do |post| %>
  <article>
    <h2><%= post.title %></h2>
    <p><%= post.body %></p>
    <ul class="tags">
      <% post.tags.each do |tag| %>
        <li><a href="/tags/<%= tag.id %>"><%= tag.name %></a></li>
      <% end %>
    <ul>
  </article>
<% end %> 
